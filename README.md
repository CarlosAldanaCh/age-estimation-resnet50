# age-estimation-resnet50
Age prediction from face images using TensorFlow/Keras and transfer learning (ResNet50). Includes a reproducible pipeline (data audit, fixed splits, tf.data), two-stage training (feature extraction + fine-tuning), MAE evaluation, error analysis, and exported metrics/figures.
